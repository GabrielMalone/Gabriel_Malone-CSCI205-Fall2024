If the 'data' folder isn't present, need to make it.

Graphical output on merge sort is wonky.

Tukey's might not really be tukey's. 

Fixed the stack depth algorithms and updated the insertion cut off PFD but not the other PDFs that have stack depth info (those record total recursive calls)


